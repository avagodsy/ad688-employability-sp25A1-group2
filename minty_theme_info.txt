The following details visualizations that match the Minty QMD theme.

Font: Montserrat
Graph titles: size 18
Graph text: size 14

Colors:
Primary - #78C2AD / #3D7C6A
Secondary - #F3969A / #B14E53
Success - #56CC9D
Info - #6CC3D5 / #297C8A

Apply the following format to the code.

The title should be size 18 Monserrat, and all other text should be size 14 Monserrat. Graph colors should be

data.dropna(thresh=len(data) * 0.5, axis=1, inplace=True)

# Visualize missing data
msno.heatmap(data)
plt.title("Missing Values Heatmap")
plt.show()